describe('Some logic tests', () => {
  it('Should equal false to false', () => {
    expect(false).to.equal(false);
  });

  it('Should equal true to true', () => {
    expect(false).to.equal(false);
  });

  it('Should not equal true to false', () => {
    expect(true).to.equal(false);
  });
});
