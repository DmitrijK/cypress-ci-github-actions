{
  "scripts": {
    "test:e2e:open": "cypress open",
    "test:e2e:run:all": "cypress run",
    "report:json:merge": "mochawesome-merge cypress/reports/*.json -o cypress/reports/report.json",
    "report:json:generate:html": "npx marge cypress/reports/report.json --reportDir ./ --cdn"
  },
  "devDependencies": {
    "@types/node": "^20.11.25",
    "cypress": "^13.6.6",
    "cypress-mochawesome-reporter": "^3.6.1",
    "cypress-multi-reporters": "^1.6.4",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "cypress-junit-reporter": "^1.3.1",
    "cypress-real-events": "^1.12.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}